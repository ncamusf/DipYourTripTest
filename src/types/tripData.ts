export interface TripAddOn {
  trip_id: string;
  add_on_id: string;
  start_date: string;
  end_date: string;
  n_days: number;
  n_users: number;
  place: string;
  item: string;
  detail: string;
}